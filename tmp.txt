source .venv/Scripts/activate
. .venv/Scripts/activate




which python





"파이썬 경로" -m venv .venv

pip install --upgrade pip setuptools wheel

# 4. 커널 연결
pip install ipykernel
python -m ipykernel install --user --name .venv --display-name "Python (.venv)"




# 1. 가상환경 생성
"파이썬 경로" -m venv .venv

# 2. 활성화
source .venv/Scripts/activate

# 3. pip 최신화
pip install --upgrade pip setuptools wheel

# 4. 커널 연결
pip install ipykernel
python -m ipykernel install --user --name .venv --display-name "Python (.venv)"

# 5. 기본 패키지 설치
pip install numpy pandas matplotlib seaborn scikit-learn tqdm rich

# 6. 설치 확인
python - << EOF
import numpy, pandas, sklearn
print("OK")
EOF

# 7. 작업 끝나면 비활성화
deactivate
